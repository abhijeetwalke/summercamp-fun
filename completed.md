# Completed — Finished Build Work

Tasks move here from `pending.md` **only after they land on `main`** (see the git workflow in `CLAUDE.md`). Each entry: date + one-line note + files touched.

---

*Nothing completed yet — build has not started.*
