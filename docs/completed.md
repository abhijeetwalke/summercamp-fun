# Completed — Finished Build Work

Tasks move here from `pending.md` **only after they land on `main`** (see the git workflow in `CLAUDE.md`). Each entry: date + **build duration (wall-clock)** + one-line note + files touched. Duration gives Abhi a feel for cost-per-feature — the feedback loop we learn from.

---

## Staged (on `preview`, awaiting Gate 2 — move below the line when merged to `main`)

> **Path note (2026-06-13):** the repo was reorganized — app files now live under `code/`, the six non-front-door docs under `docs/`. File paths in the older entries below predate the move; read `js/x.js` as `code/js/x.js`, `index.preview.html` as `code/index.preview.html`, etc.

### World Awareness — a new subject, built on preview (Abhi override of depth-first, 2026-06-17)

- **2026-06-17 · ~build session** — **World Awareness MVP** built as its own themed world ("Atlas" design): 6 continents, **39 interactive "explorable-explanation" lessons** (~255 quiz questions) + **6 section quizzes & badges**, a **29-country atlas** (flags/leaders/currency/government/fun-facts), dashboard (progress/streak/badges/personal-best), progressive unlocking, light/dark, accessibility. Teaching-first (lesson → quiz). Data ships as `.js` globals (runs from `file://`), browser-storage progress, no backend/auth — within the locked stack. Headless-tested (jsdom): full play-through, scoring/persistence, unlock+badge logic, all 39 lessons render — 0 errors. *Files: `code/js/wa/wa.{content,countries,state,components,engine}.js`, `code/css/wa.css`.*
- **2026-06-17** — **Integrated into the main site** (per Abhi: "it plugs into index, not a separate page"). `config.js` World land set `active:true`; `app.js` router gained a `#/world/...` branch → `WA.Engine.handle()`; `index.preview.html` loads the WA assets; CSS fully **scoped under `.wa-scope`** (dark theme via `.wa-dark`) so it can't bleed to/from the Math app; standalone `world-awareness.preview.html` + `wa.app.js` removed. Headless integration test (Math + World together): landing→World, lesson+quiz, atlas modal contained, Math still routes with no bleed — 0 errors. *Files: `code/js/config.js`, `code/js/app.js`, `code/index.preview.html`, `code/js/wa/*`, `code/css/wa.css`.*
- **2026-06-17** — **Competition-prep layer** ("mix it up" toward World Scholar's Cup + Geography Bee): a **Geography Drill** (`#/world/drill` — flags/capitals/physical, relaxed or ⏱️ timed, personal-best) and per-continent **"Big Question" debate cards** (argue both sides), placed after the lessons. *Files: `code/js/wa/wa.content.js` (bigQuestions + geoFacts), `code/js/wa/wa.engine.js`, `code/js/wa/wa.state.js`.*
- **2026-06-17** — **UI/consistency fixes** from Abhi's screenshots: pill buttons re-done as flex-centered, fixed-min-width pills (text no longer spills/overlaps) with a render-time backstop on every `.btn`; white-flag icon → 🗺️; country cards standardized to **"$1 ≈ \<local currency\>"** (dollar always on the left). Cache-busting `?v=` on WA assets in the entry file. *Files: `code/js/wa/wa.engine.js`, `code/js/wa/wa.countries.js`, `code/css/wa.css`, `code/index.preview.html`.*
- **2026-06-17** — **Docs: navigable vision viewer** — added a Table of Contents (anchor links + back-to-top) to `product_vision.md`, plus a generated **`product_vision.html`** (sticky sidebar TOC, scroll-spy, back-to-top) built from the `.md` by **`docs/build-vision-html.js`** (md stays source of truth; regenerate on edits). Consistency sweep across all docs to reflect WA built. *Files: `docs/product_vision.md`, `docs/product_vision.html`, `docs/build-vision-html.js`, `CLAUDE.md`, `docs/{deferred,completed,decisions_open,wisdom,pending}.md`.*

- **2026-06-13 · ~10 min** — Repo reorg: root `CLAUDE.md` + `docs/` (6 docs) + `code/` (app); `git mv` (history preserved); CLAUDE.md cross-refs + entry-file paths updated; doc staleness sweep (decisions_open ref, bank 30→60 comment, wisdom "16 lines", status date). *Files: CLAUDE.md, all docs moved, code/* moved, code/data/math.bank.js comment.*
- **2026-06-13 · ~40 min** — Visual/UX polish pass (Abhi live feedback): painterly **cel-shading** (light/shadow gradient, cast shadow, rim-light) + **darker palette** (deep-teal ocean, aged parchment, deeper fills); territory **label plaques** + location pills + smart label-side (overlap eliminated, verified by a collision check); in-map **route legend**; landing-land click **zooms in place** (translate moved to an outer wrapper — no more sideways lurch); **exotic-clay** Bending-Levels panel + per-row **hover info**; **runaway-clock** cap with "stepped away" message + tab-hidden pause + active-time accounting; **review-follow-through** ("due diligence") tracking on missed problems → ✓ per flagged item + Training-Record card. Verified: JS syntax + headless code execution (SVG builds) + label-collision check. Final look pending in-browser Gate review. *Files: code/js/{art,map,app,config}.js, code/css/style.css, code/data/math.bank.js, docs/{product_vision,decisions_open,pending}.md, CLAUDE.md.*

- **2026-06-12 · ~35 min** — Doc hardening: pending.md gap-fixes vs vision, ENGINE/MATH split + P0.4 plug-in contract, wisdom.md, app name, two-gate git workflow, grade-hidden rule. *Files: all seven docs.*
- **2026-06-13 · ~25 min** — Pull-and-depth pass (Abhi's live feedback): laptop-first full-bleed layout + full-viewport landing (wide canvas, whale, twin serpents, islets, second boat); missions renamed to episode chapters with teaser routes the engine honors; location names clickable everywhere (inline dotted, non-boxy); Training Record page = progress benchmark framework v1 (mastery coverage, tier handling, style profile, independence, bounce-back conversion, consistency); legend hover meanings; mission back link, Esc closes lore cards, view fade-ins. *Files: js/{journey,engine,app,map,art}.js, css/style.css, product_vision.md (3 new locked sections).*
- **2026-06-12 · ~50 min** — Visual richness pass (after Abhi's 2/10 treasure-map feedback): original SVG art library (parchment + torn edges, organic landmasses, element glyphs, terrain motifs, compass, koi/serpent/clouds/boat), illustrated Four Nations map with journey trails + X-marks, clickable locations (lore cards) + territories (element pages), glyphed location names, bending-levels panel, locked-mission ribbon, hint 💡-pop / wrong-answer shake + encouragement, basketball favicon. Headless-verified. *Files: js/art.js (new), js/map.js (rewrite), js/app.js, js/journey.js, css/style.css, index.preview.html, assets/favicon.svg.*
- **2026-06-12 · ~15 min** — Question bank batch 2: 30 → **60 questions**, balanced (tiers 18/25/17; grades 29/31; all six elements). Every question schema-validated + arithmetic spot-checked programmatically; UI smoke-retested against the larger pool. Awaiting parent spot-check (C.4). *Files: data/math.bank.js.*
- **2026-06-12 · ~12 min** — App first full pass (P0.1–P0.4, P1.1–P1.4, P2.1–P2.3, P2.5, P3.1–P3.3 initial): engine, mission play with hint ladder + click logging, results dashboard, thinking-model walkthroughs with interactive modules, journey stories, mastery/territory map, streaks + redemption, session clock + nudge, frustration sensing, rewards + collection, landing map with tactile click/sound/ambience. Verified via syntax checks, bank schema validation, 3-mission engine simulation, and a headless full-UI smoke test. *Files: index.preview.html, css/style.css, js/{config,state,engine,journey,modules,map,app}.js, data/math.bank.js.*

---

### On `main` (pushed directly per the scheduled Content-Track job — additive data, low-risk)

- **2026-06-17 · ~50 min (autonomous scheduled run)** — **Math question bank: 60 → 800** (Content Track complete). Built a **parameterized-template generator** — one factory per Common Core standard (G5: OA, NBT, NF, MD, G; G6: RP, NS, EE, G, SP) with easy/med/hard variants, **computed answers + mistake-based distractors**, two derived hints, intent, and a full thinking-model `steps[]`; cross-domain **capstones** (`avatarstate`) fill each grade to exactly 400. The original 60 starter questions are preserved verbatim. **Final mix:** 800 total = **400 Book Five + 400 Book Six**; tier **30 / 40 / 30** (240 / 320 / 240) exactly; all **26 G5 + 29 G6 standards present** (12–16 each); five style tags each 18.9–22.3% (deepdive/exploratory now have real share); element→domain map honored (water=NF/NS, earth=G·MD/G, fire=OA/EE, air=NBT/RP, spirit=SP G6-only, avatarstate=capstone). Grade is data-only — no grade/Book labels leak into any prompt. **Validation (Node harness, all gates PASS):** 800 total, 400/grade, every standard present, distinct 5-option sets with a canonical-value check (guarantees exactly one correct), no empty fields, no `${}` leaks, unique ids, element/domain consistency, spirit-only-G6; **64 numeric items independently recomputed, 0 mismatches**; `node --check` + headless load === 800. Generator/validator kept in-repo as the basis for the vision's replenishment pipeline. *Files: `code/data/math.bank.js` (+740 entries), `code/tools/math-bank/{gen,emit,validate}.js`, `code/tools/math-bank/README.md`, `docs/completed.md`, `docs/pending.md`.*

> Note: this used the usual preview→main gate's **content exception** (the scheduled job explicitly targets `main` so Netlify redeploys the larger pool). The app/UX work for World Awareness and Basketball remains on the working tree awaiting Abhi's normal Gates — untouched by this run.
