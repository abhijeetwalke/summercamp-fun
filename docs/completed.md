# Completed — Finished Build Work

Tasks move here from `pending.md` **only after they land on `main`** (see the git workflow in `CLAUDE.md`). Each entry: date + **build duration (wall-clock)** + one-line note + files touched. Duration gives Abhi a feel for cost-per-feature — the feedback loop we learn from.

---

## Staged (on `preview`, awaiting Gate 2 — move below the line when merged to `main`)

- **2026-06-18 · content (15-min lessons)** — **Every Basketball lesson expanded into a ~15-minute read at a 13-year-old level** (Abhi: "5 mins is not enough — I want him to spend 15 mins on each lesson… this is more like for an 8 year old"). All 20 lessons grew from ~300 → **~1,100–1,300 reading words each (avg ~1,181, ~4×)**, `minutes` set to **15** everywhere; expansion is narrative/context/why-it-matters/inspiration with **no new fabricated stats** (strict verified-facts allow-list; spot-checked h2 LeBron + ro4 Texas Western — all facts consistent, honest-but-age-appropriate). **Quizzes untouched** (still 6/lesson, 159 total). Added: a **word-count line at the bottom of each lesson** (`📖 N words · ~15 min lesson`, computed in-engine), and **overview reading blocks on the hub + every subsection page** (`C.intro` ~276 words + per-section `overview` ~250–300 words) so pages aren't just cards/badges. Engine: `renderLesson` word-count footer + `lessonWordCount()`; `renderHome`/`renderSection` render the new `.hw-read` blocks; small CSS for `.hw-read`/`.rwordcount`. Cache bumped `?v=2→?v=3`. Verified: `node --check` (content+engine), structural harness (4 sections, 20 lessons, all minutes=15, all quizzes=6, intro+4 overviews present), headless render (hub/section overviews show, lesson shows 15-min label + word-count footer + 21 beats, h2 quiz still plays to 100%), 0 console errors. Built by a constrained sub-agent for the prose; verified independently. *Files: `code/js/hoops/hoops.content.js`, `code/js/hoops/hoops.engine.js`, `code/css/hoops.css`, `code/index.html`, `code/index.preview.html`.*

- **2026-06-18 · promote + behavior** — **(a) Promoted Basketball to production:** `code/index.html` was missing the 5 Hardwood includes (`hoops.css` + 4 `hoops/*.js`) that only lived in `index.preview.html`, so the Netlify site (which serves `index.html`) didn't show Basketball. Copied the reviewed preview entry → `index.html` (now identical); verified headless that production loads the engine, the land is active, click → `#/hoops`, renders, 0 errors. **(b) Removed all gating in Basketball (Abhi):** `hoops.state.js` `sectionUnlocked`/`lessonUnlocked`/`reviewUnlocked` now return true unconditionally — every subsection, lesson, and review is open in any order (progress/best/trophies still track). Bumped hoops cache `?v=1→?v=2` in both entry files. Verified headless: with zero progress, all 4 sections + 20 lessons + 4 reviews unlocked, jumped straight into the last lesson + a review, no 🔒 markers, 0 errors. *Files: `code/index.html`, `code/index.preview.html`, `code/js/hoops/hoops.state.js`.*

- **2026-06-18 · content + feature (World Awareness 2.0 — autonomous scheduled run)** — **PART A — reading roughly DOUBLED across all 39 WA lessons.** Every lesson's prose lengthened to a confident 12–13-yo reading level (avg now **~545 reading words/lesson**, up from ~150–250), keeping every existing theme, beat, interactive (`order`/`hotmap`/`compare`/`scenario`/`scene`) and quiz **verbatim** — pure *add-more*: richer narrative + human detail, explicit cause-and-effect, extra historical context, more "why it still matters today," and added "Did you know?" facts + friendly guide asides. Sensitive topics (slavery, Holocaust, apartheid, Middle East/1948) kept factual, dignified, age-appropriate, evenhanded. **PART B — Visual & Engagement Glow-Up (WA only):** added an **XP & Levels** system (XP for lessons/quizzes/drills/badges — more for higher scores & first completion; an animated XP bar + explorer ladder Explorer→Scout→Navigator→Cartographer→Globetrotter→World Master; celebratory level-up confetti+toast; XP only ever increases — no-shame) and **Achievements / Trophies** (a dashboard trophy shelf: First Steps, Perfect!, Globetrotter, On Fire + Unstoppable [3- & 7-day streaks], Map Master, Completionist, Atlas Explorer, Debater — locked = silhouette, unlocked = colour + earned date, unlock pops a toast). Plus a bolder display heading face, animated/rounded progress bars, punchier card lift, a topbar **level chip**, and a stronger hero with level/XP front-and-centre. **Progress preserved:** the existing `summer-camp-world-v1` state is extended with a one-time **v2 migration** that back-fills XP and unlocks already-earned trophies from prior progress — no saved data lost. Everything scoped under `.wa-scope` (no Math/Basketball/landing bleed), vanilla JS, runs from `file://`, honors `prefers-reduced-motion`. Recent fixes re-verified intact (button-pill normalization, glossary tooltips). **Validation — jsdom harness, 42/42 PASS, 0 console errors:** old v1 state migrates with no loss + XP/trophy back-fill; fresh XP accrues, a level-up fires, trophies unlock (First Steps/Perfect/Globetrotter/Map Master/Debater); all 39 lessons render at ~350+ words wrapped in `.wa-scope`; a lesson quiz plays end-to-end to 100% awarding XP; **Math AND Basketball still route with no WA bleed**; buttons render as centered pills; `node --check` clean on all changed JS. Cache version bumped `?v=9 → v10` in both entry files. *Files: `code/js/wa/wa.content.js`, `code/js/wa/wa.state.js`, `code/js/wa/wa.engine.js`, `code/css/wa.css`, `code/index.html`, `code/index.preview.html`.*
- **2026-06-18 · content (quiz leveling)** — **Every lesson quiz leveled to 6 questions** (Abhi: keep h1 reading as-is, raise quiz counts to 6 each). Added 2 fact-checked questions to each of the 20 lessons (4→6), drawn from the expanded reading, same shape (mc/tf + options/answer/explain, hints where useful). **Lesson quiz Qs now 120 (20×6); + 39 section-review Qs = 159 total.** Validated by harness: all 20 lessons have exactly 6 Qs, every question has valid options + an in-range answer index + explanation; headless full play-throughs of a lesson in each section (h1/r2/ro4/leg3) answer all 6 via the keyed indices and score 100%, 0 console errors. Section reviews unchanged. *Files: `code/js/hoops/hoops.content.js`.*
- **2026-06-18 · content (consistency pass)** — **Leveled reading depth uniformly across all 20 Basketball lessons** (Abhi: the first pass over-favored Heart of a Champion — "I'm looking for consistency and reliability"). Brought every Records/Roots/Legends lesson up to the Heart baseline by adding fact-checked beats (e.g. Jordan's 10 scoring titles + Kareem skyhook; dynasty supporting casts; rebound/assist/steal/block guide + Rodman; three-point comebacks; Swoopes/Leslie/Cooper + Taurasi "White Mamba"; peach-basket ladder + 130-yr rules; New York Rens 88-game streak; segregation-on-the-road dignity; Texas Western underdog/El Paso; Russell defense + quote; 40+ countries; Duncan/Kobe/Hakeem; "The Shot"/Lillard 37 ft/Kobe 81; fadeaway/step-back/dunk contest/Iverson-over-Jordan; Maya Moore + Wilson/Clark). **Result: every lesson now ~235–321 reading words (avg 261, spread cut from ~200 to ~86)** — consistent across the board. Verified: `node --check` + headless render of lessons in every section (new beats present, 0 console errors). *Files: `code/js/hoops/hoops.content.js`.*
- **2026-06-18 · content** — **Basketball lessons: reading content roughly DOUBLED across all 20 lessons** (Abhi: Giannis felt too thin → "do the same across the board," keep the structure/format). Added ~3–4 new reading beats per lesson (same beat types — `lead`/`p`/`fact`/`quote`/`statline`), placed before each quiz; quizzes unchanged (still 119 Qs). New material fact-checked via web search: Giannis (Filathlitikos, 15th pick 2013, 2019&2020 MVP, 50-pt clinching Game 6, brothers Thanasis/Kostas, father Charles); LeBron (St. V–St. M "Chosen One," #1 2003, first-ever 3-1 Finals comeback 2016, I PROMISE School); Curry (Dell Curry, VT walk-on offer, Davidson 2008 run, 7th pick, ankle injuries, first unanimous MVP 2016); Iverson (mom Ann at 15, John Thompson/Georgetown, #1 1996, MVP at 6'0"); Butler (Tomball, Michelle Lambert, Tyler JC→Marquette, 30th pick 2011); Maya Moore (UConn 2009/2010, Lynx 4 titles, 2014 MVP, Irons freed after 23 yrs, married). Records/history/legends enriched with verified facts (Wilt 50.4 ppg & 48 min/game; Kareem record stood 39 yrs; 73-9 Warriors lost 2016 Finals; Russell 8-straight + Medal of Freedom + leaguewide #6 retirement; Naismith 1–0 first game + 1936 Olympics; Globetrotters Chicago/Saperstein/1948 Lakers; Jackie Robinson 1947 context; Earl Lloyd 1955 title; Glory Road; ABA 1976 merge + Dream Team 1992; Jordan 6-for-6/Flu Game; skyhook; Cooper/Comets, Cheryl Miller, Bird&Taurasi 5 golds). Heart lessons now ~255–320 words (was ~150). Verified: `node --check` + headless render of a lesson in each section (old + new content present, 0 console errors). *Files: `code/js/hoops/hoops.content.js`.*
- **2026-06-18 · bugfix** — **Basketball land click did nothing (beep, no navigation) — FIXED.** Root cause: the landing-map click navigated to `#/` + the subject **id** (`#/basketball`), but the Hardwood engine is mounted at `#/hoops`, so the router matched nothing and bounced back to the map. Fix: added a `route: "hoops"` field to the basketball subject in `config.js`, and made the landing-map enter-handler in `app.js` use `subject.route || subject.id` (general — future themed subjects can route under a theme name too). Verified headless (jsdom): clicking Basketball now navigates to `#/hoops`, the Hardwood world + all four subsections render, lesson `h1` shows 2 real Wikimedia photos with caption/credit and a working SVG fallback on image-error, and Math/World still route with no bleed. *Files: `code/js/config.js`, `code/js/app.js`.*

> **Path note (2026-06-13):** the repo was reorganized — app files now live under `code/`, the six non-front-door docs under `docs/`. File paths in the older entries below predate the move; read `js/x.js` as `code/js/x.js`, `index.preview.html` as `code/index.preview.html`, etc.

### World Awareness — a new subject, built on preview (Abhi override of depth-first, 2026-06-17)

- **2026-06-17 · ~build session** — **World Awareness MVP** built as its own themed world ("Atlas" design): 6 continents, **39 interactive "explorable-explanation" lessons** (~255 quiz questions) + **6 section quizzes & badges**, a **29-country atlas** (flags/leaders/currency/government/fun-facts), dashboard (progress/streak/badges/personal-best), progressive unlocking, light/dark, accessibility. Teaching-first (lesson → quiz). Data ships as `.js` globals (runs from `file://`), browser-storage progress, no backend/auth — within the locked stack. Headless-tested (jsdom): full play-through, scoring/persistence, unlock+badge logic, all 39 lessons render — 0 errors. *Files: `code/js/wa/wa.{content,countries,state,components,engine}.js`, `code/css/wa.css`.*
- **2026-06-17** — **Integrated into the main site** (per Abhi: "it plugs into index, not a separate page"). `config.js` World land set `active:true`; `app.js` router gained a `#/world/...` branch → `WA.Engine.handle()`; `index.preview.html` loads the WA assets; CSS fully **scoped under `.wa-scope`** (dark theme via `.wa-dark`) so it can't bleed to/from the Math app; standalone `world-awareness.preview.html` + `wa.app.js` removed. Headless integration test (Math + World together): landing→World, lesson+quiz, atlas modal contained, Math still routes with no bleed — 0 errors. *Files: `code/js/config.js`, `code/js/app.js`, `code/index.preview.html`, `code/js/wa/*`, `code/css/wa.css`.*
- **2026-06-17** — **Competition-prep layer** ("mix it up" toward World Scholar's Cup + Geography Bee): a **Geography Drill** (`#/world/drill` — flags/capitals/physical, relaxed or ⏱️ timed, personal-best) and per-continent **"Big Question" debate cards** (argue both sides), placed after the lessons. *Files: `code/js/wa/wa.content.js` (bigQuestions + geoFacts), `code/js/wa/wa.engine.js`, `code/js/wa/wa.state.js`.*
- **2026-06-17** — **UI/consistency fixes** from Abhi's screenshots: pill buttons re-done as flex-centered, fixed-min-width pills (text no longer spills/overlaps) with a render-time backstop on every `.btn`; white-flag icon → 🗺️; country cards standardized to **"$1 ≈ \<local currency\>"** (dollar always on the left). Cache-busting `?v=` on WA assets in the entry file. *Files: `code/js/wa/wa.engine.js`, `code/js/wa/wa.countries.js`, `code/css/wa.css`, `code/index.preview.html`.*
- **2026-06-17** — **Docs: navigable vision viewer** — added a Table of Contents (anchor links + back-to-top) to `product_vision.md`, plus a generated **`product_vision.html`** (sticky sidebar TOC, scroll-spy, back-to-top) built from the `.md` by **`docs/build-vision-html.js`** (md stays source of truth; regenerate on edits). Consistency sweep across all docs to reflect WA built. *Files: `docs/product_vision.md`, `docs/product_vision.html`, `docs/build-vision-html.js`, `CLAUDE.md`, `docs/{deferred,completed,decisions_open,wisdom,pending}.md`.*

### Basketball — "Hardwood", a new subject, built on preview (Abhi's 2nd depth-first override, 2026-06-17)

- **2026-06-17 · ~build session (autonomous scheduled run)** — **Basketball "Hardwood" built 100% on `preview`** as its own themed world — a sleek **dark sports-broadcast / ESPN-grade** look (deep court tones + hardwood texture + bold orange accents, crisp sans-serif, scoreboard/stat-line/trading-card motifs, big edge-to-edge photography, read-progress bar, tasteful motion) built to **read for a 14-year-old**, per Abhi's locked design directive. Teaching-first like World Awareness: read → interleaved quick-checks → end-of-subsection review. **Four subsections, 20 illustrated lessons, 119 quiz questions** (well over the 50+ target): *Heart of a Champion* (5 lessons / 29 Qs — Giannis, LeBron, Curry, Iverson+Butler, **Maya Moore** the woman's story), *The Record Books* (5 / 30), *Roots & Revolution* (6 / 34 — honest-but-age-appropriate history: Naismith 1891, Black Fives + Globetrotters, the 1950 color line, Texas Western 1966, Bill Russell, Dr. J→Magic/Bird→Jordan→global), *Legends & Greatest Moments* (4 / 26 + the **"Who's the GOAT?" Jordan-vs-LeBron debate card**). One **argue-both-sides debate card per subsection**. **Trophy Case** rewards (badge per subsection — Heart of a Champion, Record Keeper, Hardwood Historian, Hall of Famer — + day-streak + personal-best), basketball glossary tooltips (NBA/WNBA/NCAA/ABA/MVP/GOAT/YMCA), effort-not-ability & no-shame language throughout. **Home-court California** strip (original SVG Bear Flag + Warriors/Lakers banners; Curry leads the three-point lesson, LeBron anchors scoring + a grit story). **Every record fact-verified to current 2026 values via web research** (LeBron 43,440 career pts; Curry 4,248 threes — first ever past 4,000; Wilt 100; Russell 11 rings; Warriors 73–9; Kareem 6 MVPs; Taurasi 10,646 WNBA pts; A'ja Wilson first 4× WNBA MVP; Caitlin Clark all-time NCAA scoring record). **Real imagery** via **Wikimedia Commons** `Special:FilePath` (scaled + lazy-loaded) for verified marquee photos (Naismith + his 13 rules, Curry, LeBron, Giannis + Sepolia, Kobe, Kareem, Bill Russell at the 1963 March on Washington, Jordan, A'ja Wilson, Caitlin Clark) — each with **caption + credit** and a bold **Hardwood SVG fallback** (`onerror` → court/hoop/ball/trophy/jersey/banner art) so a missing image is **never a broken box**. *Files: `code/js/hoops/hoops.{content,state,components,engine}.js`, `code/css/hoops.css`.*
- **2026-06-17** — **Integrated into the main site** (mirrors the World Awareness pattern exactly): `config.js` Basketball land flipped `active:true` (the index map already had its geometry + glyph); `app.js` router gained one `#/hoops/...` branch → `HOOPS.Engine.handle()`; `index.preview.html` loads `css/hoops.css` + the four `hoops.*` scripts. CSS fully **scoped under `.hoops-scope`** (floating toast/confetti/modal layers live inside the scope) so it can't bleed to/from Math or World; **own localStorage key** `summer-camp-hoops-v1`; data ships as `.js` globals (runs from `file://`); a `role` field is kept in state for the later parent/child split. **Headless integration test (jsdom, 47/47 PASS, 0 errors):** landing map renders with Basketball **active** (no "coming soon" flag); `#/hoops` renders in its own scope (hero, California strip, 4 cards, Bear Flag); subsection + **debate-card reveal** + seen-state; lesson player + real-photo `<img>` with `Special:FilePath` src; full quiz play-through (100%, persistence, personal-best); **review quiz earns the Record Keeper trophy**; Trophy Case; **Math AND World Awareness still route with NO bleed**; back-to-Camp; **image-fallback path** (no-file → SVG, and simulated `img` error → SVG, never a broken box). *Files: `code/js/config.js`, `code/js/app.js`, `code/index.preview.html`, `code/js/hoops/*`, `code/css/hoops.css`.*

- **2026-06-13 · ~10 min** — Repo reorg: root `CLAUDE.md` + `docs/` (6 docs) + `code/` (app); `git mv` (history preserved); CLAUDE.md cross-refs + entry-file paths updated; doc staleness sweep (decisions_open ref, bank 30→60 comment, wisdom "16 lines", status date). *Files: CLAUDE.md, all docs moved, code/* moved, code/data/math.bank.js comment.*
- **2026-06-13 · ~40 min** — Visual/UX polish pass (Abhi live feedback): painterly **cel-shading** (light/shadow gradient, cast shadow, rim-light) + **darker palette** (deep-teal ocean, aged parchment, deeper fills); territory **label plaques** + location pills + smart label-side (overlap eliminated, verified by a collision check); in-map **route legend**; landing-land click **zooms in place** (translate moved to an outer wrapper — no more sideways lurch); **exotic-clay** Bending-Levels panel + per-row **hover info**; **runaway-clock** cap with "stepped away" message + tab-hidden pause + active-time accounting; **review-follow-through** ("due diligence") tracking on missed problems → ✓ per flagged item + Training-Record card. Verified: JS syntax + headless code execution (SVG builds) + label-collision check. Final look pending in-browser Gate review. *Files: code/js/{art,map,app,config}.js, code/css/style.css, code/data/math.bank.js, docs/{product_vision,decisions_open,pending}.md, CLAUDE.md.*

- **2026-06-12 · ~35 min** — Doc hardening: pending.md gap-fixes vs vision, ENGINE/MATH split + P0.4 plug-in contract, wisdom.md, app name, two-gate git workflow, grade-hidden rule. *Files: all seven docs.*
- **2026-06-13 · ~25 min** — Pull-and-depth pass (Abhi's live feedback): laptop-first full-bleed layout + full-viewport landing (wide canvas, whale, twin serpents, islets, second boat); missions renamed to episode chapters with teaser routes the engine honors; location names clickable everywhere (inline dotted, non-boxy); Training Record page = progress benchmark framework v1 (mastery coverage, tier handling, style profile, independence, bounce-back conversion, consistency); legend hover meanings; mission back link, Esc closes lore cards, view fade-ins. *Files: js/{journey,engine,app,map,art}.js, css/style.css, product_vision.md (3 new locked sections).*
- **2026-06-12 · ~50 min** — Visual richness pass (after Abhi's 2/10 treasure-map feedback): original SVG art library (parchment + torn edges, organic landmasses, element glyphs, terrain motifs, compass, koi/serpent/clouds/boat), illustrated Four Nations map with journey trails + X-marks, clickable locations (lore cards) + territories (element pages), glyphed location names, bending-levels panel, locked-mission ribbon, hint 💡-pop / wrong-answer shake + encouragement, basketball favicon. Headless-verified. *Files: js/art.js (new), js/map.js (rewrite), js/app.js, js/journey.js, css/style.css, index.preview.html, assets/favicon.svg.*
- **2026-06-12 · ~15 min** — Question bank batch 2: 30 → **60 questions**, balanced (tiers 18/25/17; grades 29/31; all six elements). Every question schema-validated + arithmetic spot-checked programmatically; UI smoke-retested against the larger pool. Awaiting parent spot-check (C.4). *Files: data/math.bank.js.*
- **2026-06-12 · ~12 min** — App first full pass (P0.1–P0.4, P1.1–P1.4, P2.1–P2.3, P2.5, P3.1–P3.3 initial): engine, mission play with hint ladder + click logging, results dashboard, thinking-model walkthroughs with interactive modules, journey stories, mastery/territory map, streaks + redemption, session clock + nudge, frustration sensing, rewards + collection, landing map with tactile click/sound/ambience. Verified via syntax checks, bank schema validation, 3-mission engine simulation, and a headless full-UI smoke test. *Files: index.preview.html, css/style.css, js/{config,state,engine,journey,modules,map,app}.js, data/math.bank.js.*

---

### On `main` (pushed directly per the scheduled Content-Track job — additive data, low-risk)

- **2026-06-17 · ~50 min (autonomous scheduled run)** — **Math question bank: 60 → 800** (Content Track complete). Built a **parameterized-template generator** — one factory per Common Core standard (G5: OA, NBT, NF, MD, G; G6: RP, NS, EE, G, SP) with easy/med/hard variants, **computed answers + mistake-based distractors**, two derived hints, intent, and a full thinking-model `steps[]`; cross-domain **capstones** (`avatarstate`) fill each grade to exactly 400. The original 60 starter questions are preserved verbatim. **Final mix:** 800 total = **400 Book Five + 400 Book Six**; tier **30 / 40 / 30** (240 / 320 / 240) exactly; all **26 G5 + 29 G6 standards present** (12–16 each); five style tags each 18.9–22.3% (deepdive/exploratory now have real share); element→domain map honored (water=NF/NS, earth=G·MD/G, fire=OA/EE, air=NBT/RP, spirit=SP G6-only, avatarstate=capstone). Grade is data-only — no grade/Book labels leak into any prompt. **Validation (Node harness, all gates PASS):** 800 total, 400/grade, every standard present, distinct 5-option sets with a canonical-value check (guarantees exactly one correct), no empty fields, no `${}` leaks, unique ids, element/domain consistency, spirit-only-G6; **64 numeric items independently recomputed, 0 mismatches**; `node --check` + headless load === 800. Generator/validator kept in-repo as the basis for the vision's replenishment pipeline. *Files: `code/data/math.bank.js` (+740 entries), `code/tools/math-bank/{gen,emit,validate}.js`, `code/tools/math-bank/README.md`, `docs/completed.md`, `docs/pending.md`.*

> Note: this used the usual preview→main gate's **content exception** (the scheduled job explicitly targets `main` so Netlify redeploys the larger pool). The app/UX work for World Awareness and Basketball remains on the working tree awaiting Abhi's normal Gates — untouched by this run.

- **2026-06-18 · Math launch fixes + map rebuild (on `main`, live at summercamp-fun.netlify.app)** — **(a) Chapter 1 was unreachable for new players** — the Missions ribbon listed only existing missions + locked future ones, so a fresh player saw "first journey awaits" then locked **Chapter 2+** with no startable Chapter 1 (the only entry was the Start button off to the side). Fixed: today's chapter now always renders as a startable **"Chapter N · ▶ today"** ribbon entry with its own Start button; locked teasers begin after it (commit `151edf1`). **(b) Retakes fold under their parent chapter** — one row per chapter with a "↻ retaken N× / retake in progress" note instead of a pile of separate in-progress rows; a retake **reuses the original chapter's route**; and you can't start a new retake while a mission is in progress. **(c) Four Nations map rebuilt** (Abhi: the old per-element islands crammed the region name + its temples onto one tiny "rock") — six **larger landmasses clustered into one continent** silhouette separated by **thin ocean cracks**; all 12 locations repositioned onto their regions (verified no label collisions, worst case); element identity now shown as **subtle inked labels** (no heavy brown badges); **location names hidden until hover** (visited stops stay named); in-map route legend lightened to parchment. Verified: `node --check` + a label-collision harness (0 collisions even with every label forced visible) + headless SVG build (no runtime errors). *Files: `code/js/{app,map,journey}.js`, `code/css/style.css`.*
