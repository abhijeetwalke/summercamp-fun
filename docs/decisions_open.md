# Decisions Open

Lean ledger. Only what needs Abhi's input. Decided items live in `product_vision.md`. (This file tracks PRODUCT decisions only — build-work tasks live in `pending.md`.)

## Pending from Abhi

**Nothing pending. Math is fully baked and ready to build.** ✅

The last open item — World Map mastery mechanics — is now locked: **80% first-click rate across a domain** = mastery (bumps bending level Apprentice → Adept and turns the territory green), with a minimum-attempts guard so a small sample can't fake it.

*(Upper-tier thresholds (Adept → Master) and the exact minimum-sample count are architect defaults, tunable from real data — not blocking the build.)*

## Operating Notes (not decisions — for later, when the app is live)

- **Tier-ramp weekly tuning:** the ramp schedule is already locked. This is just *how to run it* once he's using the app — if his first-click rate on a tier is >85%, make it harder faster; if <60%, ease off. No action needed now.
- **Upper-tier mastery & sample-size tuning:** watch whether 80% / ~15–20 attempts feels right once he's playing; adjust thresholds from the data.

## Parked (deliberately deferred, not pending)

- **World Awareness** — ✅ **no longer parked: BUILT on preview** (2026-06-17, Abhi override of depth-first). Full spec in product_vision.md; build log in completed.md; build/decision trail in the log below. Remaining open Q (later, not blocking): "what else makes an 11-yo smart?" and the competition-prep follow-ups (per-lesson map "where did this happen", collaborative-writing spark, annual theme).
- **Accounts/auth** — build without login first; add parent/child views later.
- **Video solutions** — superseded by interactive modules; revisit only for a specific on-point, commercial-free video.

## Decision Log

- 2026-06-12: Diagnostic-first, not a learning experience. MCQ with clicks-to-correct as core signal.
- 2026-06-12: Pools: 200 advanced-5th + 200 6th, even Common Core spread. Daily Mission: 25 → **20 problems**, Mon–Fri.
- 2026-06-12: Output = soft spots (World Map heat map + click-distribution dashboard linking to flagged problems), no summaries.
- 2026-06-12: Themes: Naruto, One Piece, Avatar, Pokémon (San Diego Safari removed). One subject = one theme. **Math = Avatar.**
- 2026-06-12: Naming: Missions / Books / Nations-elements / World Map. Student = the Avatar. Element-to-domain mapping locked.
- 2026-06-12: Pull-don't-push: easy early Missions, Tier 1/2/3 tags, static ramp toward flow. Frictionless sign-up.
- 2026-06-12: Feedback language: effort-based, never ability-based.
- 2026-06-12: Attempt counts visible: live per-question counter + Mission-level running tally + dashboard.
- 2026-06-12: Closed loop: thinking-model solutions per question, interactive format. **Interactive concept modules locked** (number lines, fraction bars, etc. embedded in walkthroughs).
- 2026-06-12: Hints on first miss; hint efficacy tracked.
- 2026-06-12: Replenishment: variants (same problem, new numbers), neuroplasticity-informed; pool grows 200 → ~2,000; constrained randomization; diversity within sets and across the week.
- 2026-06-12: Design ownership: hint ladder, authoring, replenishment pipeline, ramp default, frustration sensing delegated to architect.
- 2026-06-12: **Approved (walked through 1:1):** hint escalation ladder, replenishment pipeline (parameterized templates), tier-ramp default schedule, frustration sensing v1. Hint authoring rides with the ladder.
- 2026-06-12: **Streaks locked:** Mon–Fri streak tracked & shown. Miss a day → his choice: 5-problem redemption set to keep it alive, or let it reset. No judgment. Redemption logged separately from diagnostic of record.
- 2026-06-12: **Theme assignments locked:** One Piece = world awareness, Pokémon = science/data, Naruto = logic/puzzles. Named only — design later.
- 2026-06-12: **Build strategy locked: depth-first.** Fully bake math before any other subject moves.
- 2026-06-12: Mission size flexible: **~20 problems** (18–22 fine), Mon–Fri.
- 2026-06-12: **Mission stories locked:** every Mission is a self-contained journey (place A → place B in the Avatar world); each problem is a challenge en route. Story = wrapper, problems stay rigorous; template-generated per Mission.
- 2026-06-12: **Reward system locked:** end-of-Mission rewards in theme-native currencies (Avatar = Pai Sho tiles, One Piece = treasure, Pokémon = badges, Naruto = ranks). Earned via completion + effort metrics (first-click streaks, hint-free, bounce-backs). Rewards never gate progress.
- 2026-06-12: **UI locked:** four subjects in top nav; left side-panel ribbon of Missions; sequential unlock (one open at a time); past Missions viewable + retakeable with visible attempt counts (first attempt = diagnostic of record). Theme-native palettes; simple, clear fonts — never fancy.
- 2026-06-12: **5 answer options on every question, all tiers, always** (1-in-4 second-click guess keeps the signal honest).
- 2026-06-12: **Progression payoffs approved:** bending levels per element (Apprentice→Adept→Master) + territory unlocks; World Map visibly accrues. Tiles = currency, levels = status; both effort-driven.
- 2026-06-12: **Interactive modules replace videos** (videos parked).
- 2026-06-12: **Session timing approved:** ~20 problems budgeted ~40–60 min (no endless sessions); clock top-right with on/off toggle (his choice); elapsed time always recorded; results dashboard shows time. Goal: don't sit at the screen forever.
- 2026-06-12: **Future load balancing:** ~20/day holds while math is the only subject; drops to ~15 then ~10 per subject as others (world awareness, science, logic, basketball) come online.
- 2026-06-12: **Basketball added as its own subject** — themed simply as basketball (no franchise wrapper); quiz mode (greats, US/world record holders, milestones). Named only; math baked first.
- 2026-06-12: **Question sourcing locked:** initial 400 = mix of hand-curated (Kangaroo, AMC, RSM) + LLM-generated vs. Common Core, validated + spot-checked.
- 2026-06-12: **Pedagogical style tags locked** (shown bottom-right): Visual/Model-based (Singapore), Insight/Puzzle (Kangaroo/AMC/olympiad), Deep-dive/One-problem-many-ways (Japanese sansu), Exploratory/Conceptual, Standards/Procedural (US). Which styles click = its own diagnostic. (Finnish framing dropped — it was teaching culture, not a problem style; its spirit already lives in the no-shame/effort-based design.)
- 2026-06-12: **Domain mapping corrected for full Common Core coverage:** added **Spirit/Energy** element = Statistics & Probability (G6); **Avatar State** is now a pure cross-domain capstone (no longer a dumping ground). All 5 G5 + 5 G6 domains have a home.
- 2026-06-12: **Pool bumped 400 → 800** (400/grade) for ~14 questions/standard across styles × tiers. Top-tier, no sloppiness; multi-session build (S1 = bank, S2 = app). Max plan.
- 2026-06-12: **World Awareness brain-dump captured (NOT built; math first):** may be its own theme; clean/positive tone; learning-center reading gates 10/day diagnostics drawn only from read chapters; per-country cards for top ~50 economies (flag, leader-positive note, population/% world, currency vs USD ballpark, govt system, specialty/tourism, fun facts e.g. Vatican City); open Q logged — "what else makes an 11-yo smart."
- 2026-06-12: **Show your work locked:** physical scratch notebook/whiteboard beside the app; not a pure clicking game. Don't hold back hard multi-step questions — work happens off-screen, click records result. Photo-of-work upload parked.
- 2026-06-12: **Grade scope:** keep ALL of Book Five (easy/med/hard) despite RSM acceleration — fast wins on easy = pull. Easy→med→hard ratios kept as-is. Fallback (post-launch tuning, not pre-build): hardest tier of Book Five + lean into Book Six if he burns through easy.
- 2026-06-12: **Platform-wide vs subject-specific rules separated:** tiers/ramp, 5 options, clicks logging, hint ladder, closed loop, streaks, timing, frustration, rewards, progression, UI shell, pull-don't-push, style-tag concept = platform (reusable engine). Avatar frame, element→domain map, math style tags, math bank, journey routes = subject-specific.
- 2026-06-12: **Accounts/auth deferred:** build whole thing on one site, NO login first. Eventual model = separate parent (sees all) + child (focused subset) views over same data. Design data model with a parent/child `role` field for cheap later gating; actual auth comes after core app works.
- 2026-06-12: **World Map mastery mechanics LOCKED:** mastery = **80% first-click rate** across all questions in a domain → bumps bending level (Apprentice→Adept) and turns territory green (gray→amber→green). Minimum-attempts guard (~15–20) so small samples can't fake it. Bounce-backs count toward the rate. Upper-tier bars (Adept→Master, ~90%) = architect default, tunable.
- 2026-06-12: ✅ **MATH FULLY BAKED** — vision complete, ready to build. Build phasing: S1 = 800-question bank, S2 = app shell + wiring.
- 2026-06-12: **Landing map locked:** 4–6 asymmetric continents drifting together (fissures, ocean edges, jagged-puzzle feel); one land per subject; Math = "Airbender (Math)". Tactile click (visual press/spring + crack-snap + click sound). Inactive lands clickable but "Coming Soon", no nav. Active land zooms in.
- 2026-06-12: **Ambient effects locked:** occasional bird/V across screen + gentle ocean tides; classy/restrained, never in-your-face. Hard perf guardrails (CSS/SVG/light-canvas only, non-blocking, reduced-motion, pause when backgrounded). Added global principle: **agile beats pretty**.
- 2026-06-12: **Tech stack locked:** plain HTML/CSS/JS, no framework/backend, 100% free. Static JSON questions; browser-storage progress; free static hosting (GitHub/Cloudflare Pages). Per-device tradeoff → cloud sync deferred.
- 2026-06-12: **pending.md restructured** into dependency-ordered phases (P0 foundation → P1 vertical slice → P2 full experience → P3 landing/polish + parallel content track) with done-definitions per phase, for clean Cowork pickup.
- 2026-06-12: File convention: `product_vision.md` (locked, vivid) + `decisions_open.md` (lean).
- 2026-06-12: **Platform engine scope approved:** all [ENGINE] (subject-agnostic) infrastructure may be fully built now as part of the Math ship — config-driven, zero Avatar references in engine code. Subject plug-in contract added (pending.md P0.4). Scope otherwise stays Math-only.
- 2026-06-12: **Grade hidden from child UI (locked):** never show 5th vs 6th grade (no Book labels on questions); grade is data-only — tracking, ramp tuning, future parent view. Vision updated (Daily Mission Flow).
- 2026-06-12: **Doc hygiene:** keep all docs in mint condition — each fact lives in exactly one document; Abhi owns catching content overlap.
- 2026-06-12: **App name locked: "Summer Camp."**
- 2026-06-12: **Git workflow locked: preview-first, two gates.** Discuss → finalize → explicit approval → commit to `preview` → "preview looks good / ready for main?" → yes → merge to `main`. pending→completed moves only when on `main`. Repo initialized (`main` = docs baseline, work on `preview`).
- 2026-06-12: **Visual richness bar locked** (after Abhi's treasure-map feedback — first map pass rated 2/10): hand-illustrated feel everywhere — amorphous landmasses, parchment, element iconography + terrain motifs, journey trails, compass, sea creatures, ambient life; no dead whitespace. Vision updated (UI section).
- 2026-06-12: **Vision is living, not frozen:** product-level signal in Abhi's messages goes straight into product_vision.md (+ decision log line); writing facelifts welcome.
- 2026-06-12: **Exploration clicks:** map locations and territories are clickable — location lore cards (glyph + lore + journeys through) and per-element pages (level, history, places). Location names always carry glyphs/vibe, never flat text.
- 2026-06-12: **Laptop-first, full-bleed locked:** custom-cut for MacBook/laptop screens; landing map = full-viewport scene; no mobile requirement; no dead margins. Vision updated.
- 2026-06-12: **Missions = named chapters + journey teasers locked:** episode-style names ("Chapter 4 · The Road to Omashu"), locked future missions shown as real named route teasers the engine later honors. Vision updated (Daily Mission Flow).
- 2026-06-12: **Progress benchmark framework approved (architect design, evolves with Abhi):** Training Record page (streak chip opens it) — mastery coverage, first-try rate, tier handling (T2≥80% grade-solid, T3≥60% competition-ready), style profile, independence (hint-free + hint efficacy), bounce-back conversion, consistency; grade frontier computed but parent-only. Vision: new "Progress Benchmarks" section.
- 2026-06-12: **Map exploration depth:** every location name everywhere is a clickable lore link (inline dotted style, never boxy buttons); legend terms carry hover explanations; Esc closes lore cards.
- 2026-06-12: **Hint/try-again micro-animations:** lightbulb pop on hint reveal, shake + encouragement line on a wrong click. Favicon = basketball (summer-camp vibe in the Chrome tab).
- 2026-06-12: **`wisdom.md` created** — 16-line distilled project soul; added to CLAUDE.md reading order (read before any build work; conflicts resolve toward wisdom, flagged to Abhi). Rich-content mandate captured: the 800 questions cannot be cheap; app may be shared with his son's friends.
- 2026-06-13: **Repo reorganized (locked):** root `CLAUDE.md` + `docs/` (the six other docs) + `code/` (app entry + `css/ js/ data/ assets/`). `git mv` preserved history; doc cross-refs + entry-file paths updated to `code/index.preview.html`.
- 2026-06-13: **Doc staleness sweep:** fixed `decisions_pending.md`→`decisions_open.md` ref in vision; bank header comment 30→60 (it holds 60); "15 lines"→"16 lines" for wisdom.md; status date bumped.
- 2026-06-13: **Visual polish locked (Abhi: "childish, not polished anime"):** painterly **cel-shading** (light/shadow gradients, cast shadow, rim-light), **darker richer palette** (deep-teal ocean, aged parchment, deeper fills), carved **label plaques** ending name/meta/location overlap, in-map **route legend** (dashed = journeys, ✕ = end), land click **zooms in place** (no lurch). Vision updated (UI section).
- 2026-06-13: **Exotic-clay panels + hover info locked:** Bending Levels (and similar feature panels) get a sculpted clay surface; every Bending-Level row shows hover info (domain + what the level/% means). Vision updated.
- 2026-06-13: **Review follow-through ("due diligence") metric locked:** for missed challenges, track whether he walked the full step-by-step thinking model; surfaced per-problem (✓) and as a Training-Record rollup. Vision updated (Progress Benchmarks).
- 2026-06-13: **Runaway-clock guard locked:** clock counts only active time, pauses when tab hidden, hard-stops after ~2h with a "stepped away" message (config `clockRunawayMin`). Vision updated (Session Timing).
- 2026-06-13: **World Awareness — competition-prep layer BUILT (preview).** First "mix it up" build landed in `code/`: a **Geography Drill** (`#/world/drill`) — flags / capitals / physical-geography, **relaxed or ⏱️ timed** (Scholar's-Challenge flavor), questions generated from the country atlas + a new `geoFacts` pool, with a personal-best tracked in state and shown on the dashboard; and per-continent **"Big Question" debate cards** (`bigQuestions` map, argue-both-sides reveal, WSC Team-Debate flavor) on each continent page. New home CTA + dashboard stat + recent-activity entry. New UI is inline-styled (cache-proof). Tested headless (jsdom): drill setup + timed run + each topic, Big-Question reveal, dashboard stat, Math untouched — 0 errors; full content + integration suites still green. Remaining (optional, documented): per-lesson map "where did this happen", collaborative-writing spark, annual-theme framing.
- 2026-06-13: **World Awareness — competition alignment added to the vision ("mix it up").** Abhi wants the app to double as gentle, early prep for two internationally-available competitions: **World Scholar's Cup (the lead target — Junior Division ~ages 10–13; theme-based; debate/writing/MCQ/multimedia; ladders to Yale; runs in India + Prague/Europe + worldwide)** and the **International Geography Championships / Geography Bee (Intermediate division = grades 5–6; free online qualifying exam; US/Europe/Asia incl. India via Xpress Minds)**. Keep all current content; layer on: geography depth (capitals/flags/physical features + map "where did this happen" questions + a map/capitals drill), WSC-style "Big Question" debate prompts + current-events bridges + cross-subject tie-ins, and quiz-style variety (identify-the-flag/map multimedia, optional timed MCQ round) — all optional and effort-framed. Fully documented in product_vision.md → "Competition Alignment & External Resources" (with links). **Spec only; content/feature build is a follow-up, not yet in `code/`.**
- 2026-06-13: **World Awareness MVP BUILT — now INTEGRATED into the main site (preview, awaiting review).** Abhi gave an explicit "no waiting, build it all" override of depth-first, and clarified it must **plug into `index.preview.html`**, not be a separate page. So WA is the **World land on the landing map**: `config.js` flips `world` to `active:true`; the map's existing active-land flow routes to `#/world`; `app.js`'s router gained one branch — `#/world/...` → `WA.Engine.handle()` — keeping the main router as the single source of routing. WA renders into `#app` as its own themed world. Files under `code/`: `css/wa.css` (fully **scoped under `.wa-scope`** so its design system can't bleed into the Math app, and Math's globals can't bleed in — dark theme is a `.wa-dark` class on that wrapper) and `js/wa/wa.{content,countries,state,components,engine}.js` (data as `.js` globals, runs from `file://`, no backend/auth, separate localStorage key `summer-camp-world-v1`). The standalone `world-awareness.preview.html` + `wa.app.js` were **removed** (no longer separate). Content: 6 continents, **39 interactive lessons** (~255 quiz questions) + 6 section quizzes with badges, teaching-first lesson→quiz, explorable-explanation beats (animated SVG, drag-to-order timelines, click-to-reveal maps, compare panels, scenarios), a **29-country atlas**, dashboard (progress/streak/badges/personal best), progressive unlocking, light/dark, accessibility. Tested headless (jsdom) **integrated**: landing map renders with World active, enter `#/world` → WA in `.wa-scope`, full lesson+quiz play-through with scoring/persistence, atlas modal stays inside the scope, **Math still routes and renders with no WA bleed**, back-to-Camp works — 0 errors. **Not committed to git** (Math's preview→main gates untouched). Open follow-ups: decide if WA should share the platform engine vs. its own (currently its own); then fold into pending/completed via the normal gates.
- 2026-06-13: **World Awareness — all three forks LOCKED; build-ready on spec (gated only by depth-first).**
  - **Spine = teaching-first.** Lesson first, quiz follows; per-lesson quizzes + a section-level quiz after a group of lessons (e.g., 10 lessons → 1 section quiz). Intentionally differs from Math's diagnostic-first; subjects may differ.
  - **Delivery = interactive "explorable explanations"** ("most sophisticated way within this environment"). Scroll-driven, manipulable lessons. Rendering palette (reliability-first): animated SVG default → Canvas/WebGL for rich scenes/simulations → interactive widgets → pre-rendered media (Lottie/short clips) for cinematic moments only. Stays in locked stack: vanilla HTML/CSS/JS, **no framework, no backend, no auth** (CDN libs ok), browser-storage progress, static free hosting; reusable lesson engine so lessons are content+config. Cost accepted: highest authoring effort per lesson.
  - **Structure = continent/lesson backbone + per-country cards.** ~40 lessons across 6 continents is the spine; per-country cards (flags, currencies, leaders, etc.) survive as a feature inside it.
- 2026-06-13: **World Awareness full vision captured (placeholder replaced):** continent-based (~40 animated lessons across 6 continents + section quizzes, badges, parent involvement) folded into product_vision.md; prior per-country/reading-gated capture preserved beneath it. Still deferred — no build until Math ships. Three structural differences (teaching-first vs diagnostic-first; video/backend/auth/Firebase vs static no-backend stack; continent-lessons vs per-country cards) logged as build-time reconciliation, explicitly **not blockers** (Abhi: "we'll build the web app out and then see if there's even a need for alignment").
- 2026-06-17: **Doc consistency sweep (WA built).** Brought every doc in line with the as-built reality after World Awareness shipped on preview: CLAUDE.md status + intro + deferred-ref updated; product_vision.md WA heading "NOT BEING BUILT"→"BUILT", Themes table One Piece→its own "Atlas" theme, reward currency row, Index Page active/inactive lands; deferred.md graduated WA out; completed.md logged WA build entries (Staged/preview); wisdom.md depth-first override note; pending.md scope note. Regenerated docs/product_vision.html from the updated md. Historical log entries (06-12/06-13) left intact as the record of what was decided then.
